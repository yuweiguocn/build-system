<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.feature1" android:versionCode="1">
<!-- do not change the versionCode above, as it is overridden by the one from the base manifest -->

    <dist:module dist:onDemand="true" dist:title="@string/feature1_title">
      <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".FeatureActivity" android:label="Feature Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
